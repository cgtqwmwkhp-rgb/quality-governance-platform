name: Deploy to Azure Staging

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      reason:
        description: 'Reason for manual deployment'
        required: false
        type: string
        default: 'Manual trigger'

env:
  ACR_NAME: ${{ secrets.ACR_NAME }}
  AZURE_WEBAPP_NAME: ${{ secrets.AZURE_WEBAPP_NAME }}
  IMAGE_NAME: quality-governance-platform
  # Health check configuration - enterprise grade
  HEALTH_CHECK_MAX_ATTEMPTS: 30
  HEALTH_CHECK_INTERVAL: 10

jobs:
  build-and-deploy:
    name: Build and Deploy to Staging
    runs-on: ubuntu-latest
    outputs:
      deployment_id: ${{ steps.generate-id.outputs.deployment_id }}
      image_tag: ${{ steps.build.outputs.image_tag }}
    
    steps:
      - name: Generate deployment ID
        id: generate-id
        run: |
          DEPLOYMENT_ID="staging-$(date +%Y%m%d-%H%M%S)-${{ github.run_number }}"
          echo "deployment_id=$DEPLOYMENT_ID" >> $GITHUB_OUTPUT
          echo "📋 Deployment ID: $DEPLOYMENT_ID"

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log deployment info
        run: |
          echo "## 🚀 Staging Deployment Started" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Item | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| **Deployment ID** | ${{ steps.generate-id.outputs.deployment_id }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Trigger** | ${{ github.event_name }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Actor** | ${{ github.actor }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Branch** | ${{ github.ref_name }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Commit** | \`${{ github.sha }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Reason** | ${{ github.event.inputs.reason || 'Push to main' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Timestamp** | $(date -u +"%Y-%m-%dT%H:%M:%SZ") |" >> $GITHUB_STEP_SUMMARY

      - name: Login to Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Login to ACR
        run: az acr login --name ${{ env.ACR_NAME }}

      - name: Build and push Docker image
        id: build
        run: |
          IMAGE_TAG="${{ env.ACR_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}:${{ github.sha }}"
          IMAGE_LATEST="${{ env.ACR_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}:latest"
          
          echo "🔨 Building image: $IMAGE_TAG"
          docker build -t $IMAGE_TAG -t $IMAGE_LATEST .
          
          echo "📤 Pushing images..."
          docker push $IMAGE_TAG
          docker push $IMAGE_LATEST
          
          echo "image_tag=$IMAGE_TAG" >> $GITHUB_OUTPUT
          echo "IMAGE_TAG=$IMAGE_TAG" >> $GITHUB_ENV
          
          echo "✅ Image built and pushed successfully"

      - name: Capture expected image digest
        run: |
          EXPECTED_TAG="${{ env.ACR_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}:${{ github.sha }}"
          EXPECTED_DIGESTS=$(az acr repository show-manifests \
            --name ${{ env.ACR_NAME }} \
            --repository ${{ env.IMAGE_NAME }} \
            --query "[?tags[?@=='${{ github.sha }}']].digest" \
            -o tsv)
          EXPECTED_DIGESTS_CLEAN=$(printf "%s\n" "$EXPECTED_DIGESTS" | sed '/^$/d' | sort -u)
          EXPECTED_DIGEST_COUNT=$(printf "%s\n" "$EXPECTED_DIGESTS_CLEAN" | wc -l | tr -d ' ')
          if [ "$EXPECTED_DIGEST_COUNT" -ne 1 ]; then
            echo "❌ Expected exactly 1 digest for tag ${{ github.sha }}, got $EXPECTED_DIGEST_COUNT"
            exit 1
          fi
          EXPECTED_DIGEST=$(printf "%s\n" "$EXPECTED_DIGESTS_CLEAN" | head -n 1 | tr -d '\r')
          if [ -z "$EXPECTED_DIGEST" ]; then
            echo "❌ Failed to resolve image digest for ${EXPECTED_TAG}"
            exit 1
          fi
          case "$EXPECTED_DIGEST" in
            sha256:*) ;;
            *)
              echo "❌ Unexpected digest format: $EXPECTED_DIGEST"
              exit 1
              ;;
          esac
          
          IMAGE_DIGEST_REF="${{ env.ACR_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}@${EXPECTED_DIGEST}"
          EXPECTED_TAG_SAFE="${{ env.IMAGE_NAME }}:${{ github.sha }}"
          
          echo "EXPECTED_TAG=$EXPECTED_TAG" >> $GITHUB_ENV
          echo "EXPECTED_DIGEST=$EXPECTED_DIGEST" >> $GITHUB_ENV
          echo "IMAGE_DIGEST_REF=$IMAGE_DIGEST_REF" >> $GITHUB_ENV
          echo "EXPECTED_TAG_SAFE=$EXPECTED_TAG_SAFE" >> $GITHUB_ENV

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ env.AZURE_WEBAPP_NAME }}
          images: ${{ env.IMAGE_DIGEST_REF }}

      - name: Configure App Service environment
        run: |
          echo "🔧 Configuring App Service with Key Vault references..."
          
          # Ensure managed identity is enabled
          IDENTITY_ID=$(az webapp identity show \
            --name ${{ env.AZURE_WEBAPP_NAME }} \
            --resource-group rg-qgp-staging \
            --query principalId -o tsv 2>/dev/null || echo "")
          
          if [ -z "$IDENTITY_ID" ]; then
            echo "   📌 Enabling managed identity..."
            az webapp identity assign \
              --name ${{ env.AZURE_WEBAPP_NAME }} \
              --resource-group rg-qgp-staging \
              --output none
            
            IDENTITY_ID=$(az webapp identity show \
              --name ${{ env.AZURE_WEBAPP_NAME }} \
              --resource-group rg-qgp-staging \
              --query principalId -o tsv)
            
            # Wait for identity propagation
            sleep 30
          fi
          echo "   ✅ Managed identity: $IDENTITY_ID"
          
          # Always ensure Key Vault access is configured
          echo "   🔑 Ensuring Key Vault access..."
          az keyvault set-policy \
            --name kv-qgp-staging \
            --object-id $IDENTITY_ID \
            --secret-permissions get list \
            --output none 2>/dev/null || echo "   ⚠️ Key Vault policy already set or cannot be updated"
          
          # Fetch secrets from Key Vault
          echo "   🔐 Fetching secrets from Key Vault..."
          DB_URL=$(az keyvault secret show --vault-name kv-qgp-staging --name DATABASE-URL --query value -o tsv)
          SECRET_KEY_VAL=$(az keyvault secret show --vault-name kv-qgp-staging --name SECRET-KEY --query value -o tsv)
          JWT_SECRET_VAL=$(az keyvault secret show --vault-name kv-qgp-staging --name JWT-SECRET-KEY --query value -o tsv)
          
          if [ -z "$DB_URL" ]; then
            echo "   ❌ Failed to retrieve DATABASE_URL from Key Vault"
            exit 1
          fi
          echo "   ✅ Secrets retrieved successfully"
          
          # Configure app settings with direct values
          echo "   📝 Setting application configuration..."
          az webapp config appsettings set \
            --name ${{ env.AZURE_WEBAPP_NAME }} \
            --resource-group rg-qgp-staging \
            --settings \
              APP_ENV="staging" \
              BUILD_SHA="${{ github.sha }}" \
              BUILD_TIME="$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
              DATABASE_URL="$DB_URL" \
              SECRET_KEY="$SECRET_KEY_VAL" \
              JWT_SECRET_KEY="$JWT_SECRET_VAL" \
              JWT_ALGORITHM="HS256" \
              JWT_ACCESS_TOKEN_EXPIRE_MINUTES="30" \
              JWT_REFRESH_TOKEN_EXPIRE_DAYS="7" \
              LOG_LEVEL="INFO" \
              CORS_ORIGINS='["https://${{ env.AZURE_WEBAPP_NAME }}.azurewebsites.net"]' \
              DATABASE_ECHO="false" \
              WEBSITES_PORT="8000" \
              AZURE_CLIENT_ID="${{ secrets.AZURE_AD_CLIENT_ID }}" \
              AZURE_TENANT_ID="${{ secrets.AZURE_AD_TENANT_ID }}" \
            --output none
          
          echo "   ✅ Application settings configured"
          
          # Restart the app to apply new settings
          echo "   🔄 Restarting application..."
          az webapp restart \
            --name ${{ env.AZURE_WEBAPP_NAME }} \
            --resource-group rg-qgp-staging
          
          echo "✅ App Service configuration complete"

      - name: Run database migrations
        run: |
          echo "🗃️ Running migrations via Azure Container Instance..."
          
          # Fetch secrets into variables
          ACR_USERNAME=$(az acr credential show --name ${{ env.ACR_NAME }} --query username -o tsv)
          ACR_PASSWORD=$(az acr credential show --name ${{ env.ACR_NAME }} --query "passwords[0].value" -o tsv)
          DB_URL=$(az keyvault secret show --vault-name kv-qgp-staging --name DATABASE-URL --query value -o tsv)
          SECRET_KEY_VAL=$(az keyvault secret show --vault-name kv-qgp-staging --name SECRET-KEY --query value -o tsv)
          JWT_SECRET_VAL=$(az keyvault secret show --vault-name kv-qgp-staging --name JWT-SECRET-KEY --query value -o tsv)
          
          # Validate secrets
          if [ -z "$DB_URL" ]; then
            echo "❌ ERROR: Failed to retrieve DATABASE_URL from Key Vault"
            exit 1
          fi
          echo "✅ Successfully retrieved secrets from Key Vault"
          
          CONTAINER_NAME="qgp-migration-${{ github.run_number }}"
          
          # Create container with secure environment variables
          az container create \
            --name $CONTAINER_NAME \
            --resource-group rg-qgp-staging \
            --location westeurope \
            --os-type Linux \
            --image ${{ env.IMAGE_TAG }} \
            --registry-login-server ${{ env.ACR_NAME }}.azurecr.io \
            --registry-username "$ACR_USERNAME" \
            --registry-password "$ACR_PASSWORD" \
            --restart-policy Never \
            --secure-environment-variables \
              DATABASE_URL="$DB_URL" \
              SECRET_KEY="$SECRET_KEY_VAL" \
              JWT_SECRET_KEY="$JWT_SECRET_VAL" \
            --command-line "alembic upgrade head" \
            --cpu 1 \
            --memory 1
          
          echo "⏳ Waiting for migration to complete..."
          
          MAX_WAIT=300
          WAITED=0
          while [ $WAITED -lt $MAX_WAIT ]; do
            STATE=$(az container show --name $CONTAINER_NAME --resource-group rg-qgp-staging --query "containers[0].instanceView.currentState.state" -o tsv 2>/dev/null || echo "Running")
            if [ "$STATE" = "Terminated" ]; then
              echo "✅ Migration container terminated after ${WAITED}s"
              break
            fi
            echo "   Migration still running... (${WAITED}s)"
            sleep 10
            WAITED=$((WAITED + 10))
          done
          
          EXIT_CODE=$(az container show --name $CONTAINER_NAME --resource-group rg-qgp-staging --query "containers[0].instanceView.currentState.exitCode" -o tsv)
          
          echo "📋 Migration exit code: $EXIT_CODE"
          echo "📋 Migration logs:"
          az container logs --name $CONTAINER_NAME --resource-group rg-qgp-staging
          
          if [ "$EXIT_CODE" != "0" ]; then
            echo "❌ Migration failed!"
            exit 1
          fi
          
          echo "🧹 Cleaning up migration container..."
          az container delete --name $CONTAINER_NAME --resource-group rg-qgp-staging --yes
          echo "✅ Migrations completed successfully"

      - name: Verify deployment with retry
        id: verify
        run: |
          APP_URL="https://${{ env.AZURE_WEBAPP_NAME }}.azurewebsites.net"
          MAX_ATTEMPTS=${{ env.HEALTH_CHECK_MAX_ATTEMPTS }}
          INTERVAL=${{ env.HEALTH_CHECK_INTERVAL }}
          
          echo "🔍 Verifying deployment endpoint"
          echo "   Max attempts: $MAX_ATTEMPTS"
          echo "   Interval: ${INTERVAL}s"
          echo "   Total timeout: $((MAX_ATTEMPTS * INTERVAL))s"
          echo ""
          
          # Function to check endpoint with retry
          check_endpoint() {
            local endpoint=$1
            local name=$2
            local attempt=1
            
            echo "📡 Checking $name endpoint: $endpoint"
            
            while [ $attempt -le $MAX_ATTEMPTS ]; do
              HTTP_STATUS=$(curl -s -o /tmp/response.txt -w "%{http_code}" --max-time 30 "$endpoint" 2>/dev/null || echo "000")
              
              if [ "$HTTP_STATUS" = "200" ]; then
                echo "   ✅ Attempt $attempt: $name check PASSED (HTTP $HTTP_STATUS)"
                return 0
              elif [ "$HTTP_STATUS" = "000" ]; then
                echo "   ⏳ Attempt $attempt/$MAX_ATTEMPTS: Connection failed, retrying in ${INTERVAL}s..."
              else
                echo "   ⏳ Attempt $attempt/$MAX_ATTEMPTS: HTTP $HTTP_STATUS, retrying in ${INTERVAL}s..."
                # After 10 failed attempts, try to capture app logs for diagnostics
                if [ $attempt -eq 10 ]; then
                  echo "   📋 Capturing diagnostic logs..."
                  az webapp log download \
                    --name ${{ env.AZURE_WEBAPP_NAME }} \
                    --resource-group rg-qgp-staging \
                    --log-file /tmp/app_logs.zip 2>/dev/null || true
                fi
              fi
              
              attempt=$((attempt + 1))
              sleep $INTERVAL
            done
            
            echo "   ❌ $name check FAILED after $MAX_ATTEMPTS attempts"
            return 1
          }
          
          # Initial wait for app to start (longer wait after Key Vault config)
          echo "⏳ Initial wait (60s) for app cold start and Key Vault reference resolution..."
          sleep 60
          
          # Health check with retry
          if ! check_endpoint "$APP_URL/healthz" "Health"; then
            echo ""
            echo "❌ DEPLOYMENT VERIFICATION FAILED"
            echo "   Health endpoint did not respond with 200 after $MAX_ATTEMPTS attempts"
            echo ""
            echo "📋 Last response body:"
            cat /tmp/response.txt 2>/dev/null || echo "(empty)"
            echo ""
            echo "📋 Checking App Service status..."
            az webapp show \
              --name ${{ env.AZURE_WEBAPP_NAME }} \
              --resource-group rg-qgp-staging \
              --query "{state:state,usageState:usageState,availabilityState:availabilityState}" \
              -o json
            echo ""
            echo "📋 Recent deployment logs:"
            az webapp log show \
              --name ${{ env.AZURE_WEBAPP_NAME }} \
              --resource-group rg-qgp-staging \
              --output table 2>/dev/null || echo "(no logs available)"
            exit 1
          fi
          
          echo ""
          echo "🎉 Deployment verification PASSED!"

      - name: Capture runtime evidence and enforce determinism
        run: |
          APP_HOST=$(az webapp show \
            --name ${{ env.AZURE_WEBAPP_NAME }} \
            --resource-group rg-qgp-staging \
            --query defaultHostName -o tsv)
          APP_URL="https://${APP_HOST}"
          HEALTH_STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 30 "${APP_URL}/healthz")
          
          META_STATUS=$(curl -s -o /tmp/meta.json -w "%{http_code}" --max-time 30 "${APP_URL}/api/v1/meta/version")
          if [ "$META_STATUS" != "200" ]; then
            echo "❌ meta/version endpoint failed with HTTP ${META_STATUS}"
            exit 1
          fi
          
          BUILD_SHA=$(python - <<'PY'
import json
with open("/tmp/meta.json", "r", encoding="utf-8") as f:
    data = json.load(f)
print(data.get("build_sha", ""))
PY
          )
          
          RUNNING_IMAGE_RAW=$(az webapp config container show \
            --name ${{ env.AZURE_WEBAPP_NAME }} \
            --resource-group rg-qgp-staging \
            --query "linuxFxVersion" -o tsv)
          RUNNING_IMAGE="${RUNNING_IMAGE_RAW#DOCKER|}"
          RUNNING_IMAGE_SAFE=$(echo "$RUNNING_IMAGE" | sed -E 's#^[^/]+/##')
          
          if [ -z "$RUNNING_IMAGE" ]; then
            echo "⚠️ running image not reported; skipping digest/tag comparison"
          elif echo "$RUNNING_IMAGE" | grep -q "@sha256:"; then
            RUNNING_DIGEST=$(echo "$RUNNING_IMAGE" | sed 's/.*@//')
            if [ "$RUNNING_DIGEST" != "$EXPECTED_DIGEST" ]; then
              echo "❌ running digest mismatch: expected ${EXPECTED_DIGEST}, got ${RUNNING_DIGEST}"
              exit 1
            fi
          elif echo "$RUNNING_IMAGE" | grep -q ":"; then
            RUNNING_TAG=$(echo "$RUNNING_IMAGE_SAFE" | awk -F: '{print $NF}')
            if [ "$RUNNING_TAG" != "${{ github.sha }}" ]; then
              echo "❌ running tag mismatch: expected ${{ github.sha }}, got ${RUNNING_TAG}"
              exit 1
            fi
          else
            echo "⚠️ running image format unrecognized; skipping digest/tag comparison"
          fi
          
          if [ "$BUILD_SHA" != "${{ github.sha }}" ]; then
            echo "❌ build_sha mismatch: expected ${{ github.sha }}, got ${BUILD_SHA}"
            exit 1
          fi
          
          EVIDENCE_PATH="docs/evidence/DEPLOY_EVIDENCE_staging_${{ github.run_id }}.md"
          HOST_HASH=$(APP_HOST="$APP_HOST" python - <<'PY'
import hashlib
import os
host = os.environ.get("APP_HOST", "")
print(hashlib.sha256(host.encode("utf-8")).hexdigest() if host else "")
PY
          )
          cat <<EOF > "$EVIDENCE_PATH"
## Deploy Evidence (staging)

- run_id: ${{ github.run_id }}
- sha: ${{ github.sha }}
- expected_tag: ${EXPECTED_TAG_SAFE}
- expected_digest: ${EXPECTED_DIGEST}
- running_image: ${RUNNING_IMAGE_SAFE}
- meta_version_build_sha: ${BUILD_SHA}
- healthz_status: ${HEALTH_STATUS}
- host_sha256: ${HOST_HASH}
EOF

      - name: Upload deploy evidence artifact
        uses: actions/upload-artifact@v4
        with:
          name: deploy-evidence-staging-${{ github.run_id }}
          path: docs/evidence/DEPLOY_EVIDENCE_staging_${{ github.run_id }}.md

      - name: Deployment summary
        if: always()
        run: |
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 📊 Deployment Result" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [[ "${{ job.status }}" == "success" ]]; then
            echo "### ✅ STAGING DEPLOYMENT SUCCESSFUL" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "| Item | Value |" >> $GITHUB_STEP_SUMMARY
            echo "|------|-------|" >> $GITHUB_STEP_SUMMARY
            echo "| **Image** | \`${{ env.IMAGE_NAME }}:${{ github.sha }}\` |" >> $GITHUB_STEP_SUMMARY
            echo "| **Health Check** | ✅ Passed with retry |" >> $GITHUB_STEP_SUMMARY
          else
            echo "### ❌ STAGING DEPLOYMENT FAILED" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "Check the workflow logs for details." >> $GITHUB_STEP_SUMMARY
          fi

  # Post-deployment smoke tests
  smoke-tests:
    name: Staging Smoke Tests
    runs-on: ubuntu-latest
    needs: build-and-deploy
    if: needs.build-and-deploy.result == 'success'
    
    steps:
      - name: Run smoke tests
        run: |
          APP_URL="https://${{ secrets.AZURE_WEBAPP_NAME }}.azurewebsites.net"
          echo "🧪 Running staging smoke tests"
          
          TESTS_PASSED=0
          TESTS_FAILED=0
          
          # Test 1: Health endpoint
          echo "Test 1: Health endpoint"
          HEALTH=$(curl -s --max-time 30 "$APP_URL/healthz")
          if echo "$HEALTH" | grep -q '"status"'; then
            echo "   ✅ PASSED - Health endpoint returns valid JSON"
            TESTS_PASSED=$((TESTS_PASSED + 1))
          else
            echo "   ❌ FAILED - Invalid health response"
            TESTS_FAILED=$((TESTS_FAILED + 1))
          fi
          
          # Test 2: Readiness endpoint
          echo "Test 2: Readiness endpoint"
          READY=$(curl -s --max-time 30 "$APP_URL/readyz")
          if echo "$READY" | grep -q '"status"'; then
            echo "   ✅ PASSED - Readiness endpoint returns valid JSON"
            TESTS_PASSED=$((TESTS_PASSED + 1))
          else
            echo "   ❌ FAILED - Invalid readiness response"
            TESTS_FAILED=$((TESTS_FAILED + 1))
          fi
          
          # Test 3: API returns 401/403 for unauthenticated requests
          echo "Test 3: API authentication guard"
          AUTH_STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 30 "$APP_URL/api/v1/policies")
          if [ "$AUTH_STATUS" = "401" ] || [ "$AUTH_STATUS" = "403" ]; then
            echo "   ✅ PASSED - API correctly rejects unauthenticated requests (HTTP $AUTH_STATUS)"
            TESTS_PASSED=$((TESTS_PASSED + 1))
          else
            echo "   ⚠️ INFO - Received HTTP $AUTH_STATUS"
          fi
          
          # Test 4: OpenAPI spec available
          echo "Test 4: OpenAPI specification"
          OPENAPI_STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 30 "$APP_URL/openapi.json")
          if [ "$OPENAPI_STATUS" = "200" ]; then
            echo "   ✅ PASSED - OpenAPI spec accessible"
            TESTS_PASSED=$((TESTS_PASSED + 1))
          else
            echo "   ⚠️ INFO - OpenAPI spec returned HTTP $OPENAPI_STATUS"
          fi
          
          # Test 5: Docs endpoint
          echo "Test 5: API documentation"
          DOCS_STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 30 "$APP_URL/docs")
          if [ "$DOCS_STATUS" = "200" ]; then
            echo "   ✅ PASSED - API docs accessible"
            TESTS_PASSED=$((TESTS_PASSED + 1))
          else
            echo "   ⚠️ INFO - API docs returned HTTP $DOCS_STATUS"
          fi
          
          echo ""
          echo "📊 Results: $TESTS_PASSED passed, $TESTS_FAILED failed"
          echo ""
          
          if [ $TESTS_FAILED -gt 0 ]; then
            echo "❌ Critical smoke tests failed!"
            exit 1
          fi
          
          echo "🎉 All smoke tests passed!"

      - name: Smoke test summary
        if: always()
        run: |
          echo "## 🧪 Staging Smoke Tests" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Test | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Health Endpoint | ✅ |" >> $GITHUB_STEP_SUMMARY
          echo "| Readiness Endpoint | ✅ |" >> $GITHUB_STEP_SUMMARY
          echo "| Auth Guard | ✅ |" >> $GITHUB_STEP_SUMMARY
          echo "| OpenAPI Spec | ✅ |" >> $GITHUB_STEP_SUMMARY
          echo "| API Docs | ✅ |" >> $GITHUB_STEP_SUMMARY

  # Final notification
  notify:
    name: Deployment Notification
    runs-on: ubuntu-latest
    needs: [build-and-deploy, smoke-tests]
    if: always()
    
    steps:
      - name: Final status
        run: |
          echo "## 📊 Staging Deployment Final Status" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [[ "${{ needs.build-and-deploy.result }}" == "success" && "${{ needs.smoke-tests.result }}" == "success" ]]; then
            echo "### ✅ STAGING DEPLOYMENT SUCCESSFUL" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "The staging deployment completed successfully. Ready for production promotion." >> $GITHUB_STEP_SUMMARY
            echo ""
            echo "🎉 Staging deployment successful!"
            echo "   Deployment ID: ${{ needs.build-and-deploy.outputs.deployment_id }}"
          else
            echo "### ❌ STAGING DEPLOYMENT FAILED" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "| Stage | Result |" >> $GITHUB_STEP_SUMMARY
            echo "|-------|--------|" >> $GITHUB_STEP_SUMMARY
            echo "| Build & Deploy | ${{ needs.build-and-deploy.result }} |" >> $GITHUB_STEP_SUMMARY
            echo "| Smoke Tests | ${{ needs.smoke-tests.result }} |" >> $GITHUB_STEP_SUMMARY
            echo ""
            echo "❌ Staging deployment failed - check logs above"
          fi
