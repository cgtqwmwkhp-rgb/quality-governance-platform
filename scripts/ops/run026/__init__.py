"""Run026 ops park — C-27 least-privilege connection identity for row-level security.

Read-only preflight tooling for switching the application's database role from one
holding ``rolbypassrls`` to ``qgp_app``. Nothing in this package mutates data.
"""
