# FORCE RLS expand — document family

Path-to-10 S3/S9: ENABLE + FORCE + `tenant_isolation` (USING + WITH CHECK) on
TEN2-complete document tables:

- `document_versions`
- `controlled_documents`
- `controlled_document_versions`

Chains from `20260711_rls_act_exp`. Middleware `RLS_TABLES` catalog updated to 21.
