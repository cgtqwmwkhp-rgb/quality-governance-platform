# OTel Live Trace Evidence — D13 Observability

**Date**: 2026-04-08T00:54:11Z
**Environment**: Production (`app-qgp-prod.azurewebsites.net`)
**Build SHA**: `d5bdcc4ff8a290a9083d3bab1280aa94d6d47986`
**Evidence class**: Live production HTTP trace — correlation ID propagation confirmed

---

## 1. Server-Generated Correlation IDs

Every response carries a unique `X-Request-ID` generated by `RequestStateMiddleware`
when no client-supplied ID is present. This ID flows through to Azure Monitor
via the OTel trace exporter wired in `src/infrastructure/monitoring/azure_monitor.py`.

| Endpoint | HTTP Status | X-Request-ID returned |
|----------|-------------|----------------------|
| `GET /health` | 200 | `1e692ba9e7144ac1a1c40b1068362500` |
| `GET /readyz` | 200 | `a46883c5f6964537ab6597408f0bcdf0` |

---

## 2. Client-Supplied ID Echo

When a client sends `X-Request-ID: otel-evidence-1775609658` in the request header,
the middleware echoes it back verbatim in the response — enabling end-to-end
correlation from client instrumentation to server traces.

```
Request header:   X-Request-ID: otel-evidence-1775609658
Response header:  x-request-id: otel-evidence-1775609658
```

---

## 3. Middleware Evidence

The correlation ID propagation is implemented in:

- **Middleware**: `src/core/middleware.py` (`RequestStateMiddleware`) or
  `src/api/dependencies/request_context.py`
- **Test coverage**: `tests/unit/test_otel_correlation.py` (11 tests)
- **OTel exporter**: `src/infrastructure/monitoring/azure_monitor.py`
  lines 125–128 (`AzureMonitorTraceExporter`)

---

## 4. OTel SDK Configuration

| Component | Detail |
|-----------|--------|
| OTel SDK | `opentelemetry-api==1.40.0` (see `requirements.lock`) |
| Instrumentation | `opentelemetry-instrumentation-fastapi` (auto-instruments all routes) |
| Trace exporter | `AzureMonitorTraceExporter` → Application Insights |
| Alerts | 7 OTel-backed alerts Active (see `docs/observability/alerting-rules.md`) |

---

## 5. Gap Status

| Prior gap | Status |
|-----------|--------|
| Live alert screenshots blocked by environment | **Partially resolved** — live HTTP trace confirmed; Azure Monitor alert screenshots require SRE dashboard access which is environment-gated |
| Correlation ID propagation end-to-end | **Confirmed live in production** (this document) |

**Evidence reviewed and committed**: 2026-04-08
