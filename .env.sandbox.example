# Quality Governance Platform - Sandbox Environment Variables
# Purpose: Example configuration for local sandbox deployment
# Usage: Copy to .env.sandbox and customize if needed (docker-compose will use inline env vars)

# Application Environment
APP_ENV=development

# Database Configuration
DATABASE_URL=postgresql+asyncpg://qgp_user:qgp_sandbox_password@postgres:5432/quality_governance_sandbox
DATABASE_ECHO=false

# Security Keys (SANDBOX ONLY - NOT FOR PRODUCTION)
SECRET_KEY=sandbox-secret-key-not-for-production-use-only
JWT_SECRET_KEY=sandbox-jwt-secret-key-not-for-production-use-only

# JWT Configuration
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# Logging
LOG_LEVEL=INFO

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# PostgreSQL Container Configuration
POSTGRES_USER=qgp_user
POSTGRES_PASSWORD=qgp_sandbox_password
POSTGRES_DB=quality_governance_sandbox
